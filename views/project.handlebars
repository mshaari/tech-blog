<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Posted by {{user.name}} on {{format_date date_created}}</p>
  <form>
    <h2>Leave a comment!</h2>
    <textarea type="text" id="projectComment"/>
    <button id="submitComment">Submit</button>
    {{#if }}
  </form>
</div>

<script src="js/comment.js"></script>